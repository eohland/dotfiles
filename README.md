dotfiles
========

Install dotfiles
----------------

1) Clone the repository
```shell
git clone --recurse-submodules https://github.com/eohland/dotfiles.git ~/.dotfiles
```

2) Make the links
```
cd ~/.dotfiles
./link.sh
```
